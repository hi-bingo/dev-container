services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: dev-container:local
    stdin_open: true
    tty: true
    working_dir: /workspace
    env_file:
      - .env
    environment:
      HOME: /home/dev
    volumes:
      - .:/workspace
      - ~/.ssh:/home/dev/.ssh:ro
